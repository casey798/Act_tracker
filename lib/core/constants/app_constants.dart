import 'dart:ui';

class AppCategories {
  static const String focus = 'Focus';
  static const String social = 'Social';
  static const String rest = 'Rest';
  static const String idle = 'Idle';
}

class AppColors {
  // Focus (Red)
  static const Color focusStart = Color(0xFFFF1111);
  static const Color focusEnd = Color(0xFFFF5555);

  // Social (Gold/Orange)
  static const Color socialStart = Color(0xFFFFD700);
  static const Color socialEnd = Color(0xFFFF4500);

  // Rest (Green/Turquoise)
  static const Color restStart = Color(0xFF00FF7F);
  static const Color restEnd = Color(0xFF00CED1);

  // Idle (Blue/Purple)
  static const Color idleStart = Color(0xFF1E90FF);
  static const Color idleEnd = Color(0xFF9370DB);
}
